% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/css.R
\name{to_css}
\alias{to_css}
\title{Format colours as modern CSS color functions}
\usage{
to_css(
  value,
  from = NULL,
  to = c("oklch", "oklab", "rgb", "hsl", "hex"),
  alpha = 1,
  fallback = TRUE,
  distance = c("lab", "oklch", "rgb", "hsl")
)
}
\arguments{
\item{value}{Colour input. For \code{from = "hex"} or \code{from = "name"}, a
character vector. For numeric spaces (\code{rgb}, \code{hsl}, \code{oklch}, \code{oklab}), a
numeric vector of length 3, matrix/data frame with three columns, or a
list of such vectors.}

\item{from}{Source colour space. One of \code{"hex"}, \code{"rgb"}, \code{"hsl"}, \code{"oklab"},
\code{"oklch"}, or \code{"name"}. If \code{NULL} (default), \code{to_css()} will infer \code{"hex"}
vs \code{"name"} for character inputs.}

\item{to}{Target CSS function. One of \code{"oklch"} (default), \code{"oklab"}, \code{"rgb"},
\code{"hsl"}, or \code{"hex"}.}

\item{alpha}{Alpha channel as numbers in \verb{[0, 1]}. Recycled to match the
number of colours.}

\item{fallback}{Behaviour when \code{from = "name"} (via
\code{\link[=convert_colourspace]{convert_colourspace()}}/\code{\link[=name_to_hex]{name_to_hex()}}) or when generating names
elsewhere. \code{TRUE} (default) returns the closest named colour;
\code{FALSE} returns \code{NA}. Included for API consistency with
\code{\link[=convert_colourspace]{convert_colourspace()}}.}

\item{distance}{Distance metric used for nearest-name fallback when
applicable. Included for API consistency with \code{\link[=convert_colourspace]{convert_colourspace()}}.}
}
\value{
A character vector of CSS colors.
}
\description{
Convert colours between supported spaces and return a character vector in
modern CSS Color 4 functional notation (space-separated components with an
optional alpha channel introduced by \code{/}).
}
\examples{
to_css("red")
to_css("#ff5a3c", from = "hex", to = "oklch", alpha = 0.8)
to_css(c("#ff0000", "#00ff00"), to = "rgb", alpha = c(1, 0.5))
}
\seealso{
\href{https://evilmartians.com/chronicles/oklch-in-css-why-quit-rgb-hsl}{OKLCH in CSS: why we moved from RGB and HSL}
for a detailed explanation of why OKLCH is the recommended colour space
for modern CSS.
}
